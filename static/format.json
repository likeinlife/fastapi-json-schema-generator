{
  "properties": {
    "kind": {
      "type": "string",
      "maxLength": 32
    },
    "name": {
      "type": "string",
      "maxLength": 64
              "format": "uuid"
            }
          },
          "required":["param_a"]
        },
        "settings": {
          "type": "object",
          "properties": {
            "param_b": {
              "type": "string",
              "format": "uuid"
            }
          },
          "required":["param_b"]
        }
      },
      "required": ["specification", "settings"]
    }
  },
  "required": ["kind", "name", "version", "configuration", "description"]
}